# Dependencies and builds
node_modules
build
storybook-static
coverage
dist
.cache
.parcel-cache
tmp

# Docker files
.dockerignore
Dockerfile*
docker-compose*.yml

# Version control
.git
.gitignore
.gitattributes

# Environment files
.env*
*.log

# IDE and OS files  
.DS_Store
.vscode
.idea
*.sublime-*
.project
*.swp
*.swo
Thumbs.db
ehthumbs.db

# Documentation that doesn't need to be in main/storybook builds
docs/_site
docs/vendor
docs/.bundle
docs/.sass-cache
docs/node_modules

# Test and development files
*.test.js
*.spec.js
__tests__
.nyc_output
coverage
test-results
playwright-report
e2e/test-results

# CI/CD files
.github
.gitlab-ci.yml
.circleci
.travis.yml

# Build artifacts
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

# Large files that shouldn't be copied
*.tar.gz
*.zip
*.dmg
*.iso

# Context-specific exclusions (commented for reference)
# For main app builds - exclude storybook files:
# .storybook
# storybook-static

# For storybook builds - exclude main build:
# build

# For docs builds - exclude app builds:
# build
# storybook-static
