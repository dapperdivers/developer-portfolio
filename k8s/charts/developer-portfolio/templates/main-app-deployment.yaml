{{- if .Values.mainApp.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.mainApp.name }}
  labels:
    app: {{ .Values.mainApp.name }}
    chart: {{ include "developer-portfolio.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.mainApp.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.mainApp.name }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Values.mainApp.name }}
        release: {{ .Release.Name }}
    spec:
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "developer-portfolio.serviceAccountName" . }}
      containers:
        - name: {{ .Values.mainApp.name }}
          image: "{{ .Values.global.registry }}/{{ .Values.mainApp.image.repository }}:{{ .Values.mainApp.image.tag }}"
          imagePullPolicy: {{ .Values.mainApp.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.mainApp.service.targetPort }}
              protocol: TCP
          env:
            {{- toYaml .Values.mainApp.env | nindent 12 }}
          {{- if .Values.mainApp.healthCheck.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.mainApp.healthCheck.path }}
              port: http
            initialDelaySeconds: {{ .Values.mainApp.healthCheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.mainApp.healthCheck.periodSeconds }}
            timeoutSeconds: {{ .Values.mainApp.healthCheck.timeoutSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.mainApp.healthCheck.path }}
              port: http
            initialDelaySeconds: {{ .Values.mainApp.healthCheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.mainApp.healthCheck.periodSeconds }}
            timeoutSeconds: {{ .Values.mainApp.healthCheck.timeoutSeconds }}
          {{- end }}
          resources:
            {{- toYaml .Values.mainApp.resources | nindent 12 }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}