🎉 Developer Portfolio has been successfully deployed!

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
🌐 Your applications are available at:
  Main Portfolio: https://{{ $host.host }}
  Storybook:      https://{{ $host.host }}/storybook  
  Documentation:  https://{{ $host.host }}/docs
{{- end }}
{{- else }}
🔧 To access your applications, run these commands:

{{- if .Values.mainApp.enabled }}
  Main Portfolio:
    kubectl port-forward svc/{{ .Values.mainApp.name }} 3001:{{ .Values.mainApp.service.port }}
    Access at: http://localhost:3001
{{- end }}

{{- if .Values.storybook.enabled }}
  Storybook:
    kubectl port-forward svc/{{ .Values.storybook.name }} 6006:{{ .Values.storybook.service.port }}
    Access at: http://localhost:6006
{{- end }}

{{- if .Values.docs.enabled }}
  Documentation:
    kubectl port-forward svc/{{ .Values.docs.name }} 4000:{{ .Values.docs.service.port }}
    Access at: http://localhost:4000
{{- end }}
{{- end }}

📊 Monitor your deployment:
  kubectl get pods -l "app.kubernetes.io/instance={{ .Release.Name }}"
  kubectl get services -l "app.kubernetes.io/instance={{ .Release.Name }}"

📚 For more information:
  Chart: https://github.com/DapperDivers/developer-portfolio
  Issues: https://github.com/DapperDivers/developer-portfolio/issues