{{- if .Values.storybook.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.storybook.name }}
  labels:
    app: {{ .Values.storybook.name }}
    chart: {{ include "developer-portfolio.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.storybook.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.storybook.name }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Values.storybook.name }}
        release: {{ .Release.Name }}
    spec:
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "developer-portfolio.serviceAccountName" . }}
      containers:
        - name: {{ .Values.storybook.name }}
          image: "{{ .Values.global.registry }}/{{ .Values.storybook.image.repository }}:{{ .Values.storybook.image.tag }}"
          imagePullPolicy: {{ .Values.storybook.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.storybook.service.targetPort }}
              protocol: TCP
          {{- if .Values.storybook.healthCheck.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.storybook.healthCheck.path }}
              port: http
            initialDelaySeconds: {{ .Values.storybook.healthCheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.storybook.healthCheck.periodSeconds }}
            timeoutSeconds: {{ .Values.storybook.healthCheck.timeoutSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.storybook.healthCheck.path }}
              port: http
            initialDelaySeconds: {{ .Values.storybook.healthCheck.initialDelaySeconds }}
            periodSeconds: {{ .Values.storybook.healthCheck.periodSeconds }}
            timeoutSeconds: {{ .Values.storybook.healthCheck.timeoutSeconds }}
          {{- end }}
          resources:
            {{- toYaml .Values.storybook.resources | nindent 12 }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}