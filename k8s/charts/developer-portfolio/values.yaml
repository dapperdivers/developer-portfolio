# Global configuration
global:
  registry: ghcr.io/dapperdivers
  pullPolicy: IfNotPresent
  
# Main React Application
mainApp:
  enabled: true
  name: main-app
  image:
    repository: developer-portfolio
    tag: latest
    pullPolicy: IfNotPresent
  
  replicaCount: 2
  
  service:
    type: ClusterIP
    port: 3001
    targetPort: 3001
    
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi
      
  env:
    - name: NODE_ENV
      value: "production"
    - name: PORT
      value: "3001"
    - name: ALLOWED_DOMAINS
      value: "https://your-domain.com"
      
  healthCheck:
    enabled: true
    path: /healthz
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
    
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70

# Note: Storybook and Docs are now served from the main Express server
# at /storybook/ and /docs/ paths respectively

# Ingress configuration
ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    
  hosts:
    - host: your-domain.com
      paths:
        - path: /
          pathType: Prefix
          service: main-app
          
  tls:
    - secretName: portfolio-tls
      hosts:
        - your-domain.com

# Security
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 1000

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: false
  runAsNonRoot: true
  runAsUser: 1000

# Node selection
nodeSelector: {}
tolerations: []
affinity: {}

# Service Account
serviceAccount:
  create: true
  name: ""
  annotations: {}